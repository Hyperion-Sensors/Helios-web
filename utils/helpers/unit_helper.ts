// Removes underscores and capitalizes first letter of each word
export function convertToFahrenheit(temp: number) {
  return temp * 1.8 + 32;
}

export function convertToFeet(distance: number) {
  return distance * 3.281;
}
